{
  "name": "sample",
  "private": true,
  "scripts": {
    "dev": "slidev --open",
    "build": "slidev build",
    "export:pdf": "slidev export",
    "export:pptx": "slidev export --format pptx"
  }
}
